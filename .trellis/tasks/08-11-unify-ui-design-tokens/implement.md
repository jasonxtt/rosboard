# 执行计划：统一 UI 设计令牌与控件基类

## 第一期：令牌层

- [x] 清点高度、间距、焦点环、字号与断点现状。
- [x] 引入角色令牌并以等值 `var()` 替换字面值。
- [x] 审计媒体查询与同断点规则；有可见或级联差异的统一项保留现状并移入第五期。
- [x] 对构建前后 CSS 做解析值与级联顺序复核。
- [x] 运行前端 lint/build、Go test/vet 与本地运行验证。
- [x] 按部署门禁备份并部署到 `10.0.0.6`，验证 systemd、健康接口和嵌入资产。
- [x] 人工验收第一期通过。
- [x] 提交第一期（`574498a`）。

## 后续阶段

- [x] 第二期：药丸基类、显式控件类与 select 外观（`41c57ba`）。
- [x] 第三期：收窄表单选择器并删除由其造成的 `!important`。
- [ ] 第四期：统一主题与自动刷新选择交互。

每一期独立完成验证、部署、人工验收与提交；前一期未验收时不开始下一期。

## 暂停点

2026-08-11 完成第三期：为设置表单的文本、数字、密码和 URL 控件增加
`.settings-input`，将通用输入样式从 `.settings-form input` 收窄到显式控件类，
并为复选框、接口卡片和主题卡片保留等值的局部样式；设置表单区域的
`!important` 已清零。已通过前端 lint/build、Go test/vet、静态视觉回归和本地
运行时检查；按门禁部署到 `10.0.0.6`，备份目录为
`/opt/rosboard/backups/rosboard-ui-phase3-20260811-223755/`，远端 systemd、
健康/API 合约和嵌入 CSS 均验证通过，用户已人工验收通过。第四期保持未开始。
