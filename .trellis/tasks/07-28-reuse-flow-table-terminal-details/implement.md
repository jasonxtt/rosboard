# 实施计划：终端列表与连接详情

1. 调整终端列表设备与 IP 两列的信息层级，保持范围和排序行为。
2. 保留 RouterOS 原始连接四元组，继续按终端方向计算上传/下载。
3. 将来源 IP 与来源端口拆为独立列、排序键和筛选键。
4. 确认没有新增独立流表导航、页面、状态或 API。
5. 更新后端与前端规范，运行 Go 测试、竞态测试、前端 lint/build/audit 和 diff 检查。
6. 按项目门禁重新备份并部署到 `10.0.0.6`，验证 systemd、health 和嵌入资源后等待用户视觉验收。
7. 用户明确验收通过后再提交、归档任务并记录日志。
